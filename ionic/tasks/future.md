# Tasks — Future (deferred)

- **Voice/quick-report incident** — dictation into incident description; only if field usage shows it's needed.
- **Task voice notes** — attach audio to evidence; backend has no audio type (TaskEvidence types are image-based).
- **In-app map of today's route** — property pins + task order; backend has no route/scheduling API yet.
- **Bulk status actions** (supervisor) — select-many → reassign/batch status; backend single-transition only today.
