# Properties — Future (deferred)

- **Embedded map tiles** — system-maps intent is enough for v1; embedded map when web map view ships.
- **Property photos** — web evidence/per-property photo set; check backend before planning.
- **Full admin edit/delete on mobile** — web remains canonical (update/delete API exists if ever needed).
