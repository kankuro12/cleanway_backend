# Attendance — Current

## Done

- Module scaffolded (plan/design/future/suggestion).

## Doing

- (nothing — awaiting kickoff)

## Blocked

- Supervisor shift board + attendance event log need backend additions (`GET /attendance/events`, `GET /shifts`, `PATCH /shifts/{id}/status`).
