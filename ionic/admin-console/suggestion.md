# Admin Console — Suggestions

- Ship dashboard read-only first (no drill-down) if widgets API lands early; drill-down is polish, not core.
- Reject-with-reason should be mandatory UX (even if API treats reason optional) — cleaners need context; matches web approval flow.
- Keep the attention queue severity mapping table shared with web's (same strings) — one typo in severity names = missing pills on one platform.
