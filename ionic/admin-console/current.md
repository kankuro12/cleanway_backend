# Admin Console — Current

## Done

- Module scaffolded (plan/design/future/suggestion).

## Doing

- (nothing — awaiting kickoff)

## Blocked

- All three backend additions pending: `GET /dashboard/widgets`, `GET /approvals` + `POST /approvals/{id}/decision`, `GET /incidents` + detail. Without them the tab can't render.
