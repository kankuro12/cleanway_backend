# Admin Console — Future (deferred)

- **Schedule/calendar view** — web calendar; needs backend scheduling API (not in app scope).
- **Batch approvals** — multi-select approve; backend single-decision today.
- **Supervisor approval via push** — "task awaiting approval" push with inline approve action; requires notification action buttons (FCM data messages) — defer until push module mature.
