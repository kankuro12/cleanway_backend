# Foundation — Future (deferred)

- **Biometric login** — device biometric as primary auth after first token issue (suggestion, not required).
- **SSO** — only if customer accounts appear; backend has no SSO surface today.
- **Forced-update prompt** — version-check screen once real users exist.
- **Multi-tenant base URL selector** — only if the app ships to multiple customers (dev build uses one env var).
