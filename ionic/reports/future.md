# Reports — Future (deferred)

- **Scheduled report push** — nightly summary pushed via FCM; needs backend scheduled job + payload work.
- **Chart rendering** — report charts on mobile; generic rows/stat pairs first, charts only if a report demands it.
- **Offline report cache** — reports are read-only; defer caching beyond simple last-view cache.
