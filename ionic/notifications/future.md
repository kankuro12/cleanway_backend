# Notifications — Future (deferred)

- **Per-type subscription settings** — notification preferences (mirror web `notifications` preferences when they ship to API).
- **Local reminder scheduling** — shift-start / task-upcoming local notifications (no backend change).
- **Rich media in pushes** — image attachments (FCM support varies by platform; backend payload has no image URL today).
