# Personnel — Suggestions

- Team status refresh should be event-driven later (push on clock events) — for now 30s polling is fine (ponytail: polling, swap to push when push module lands).
- Reuse the tasks filter param `assignee_id` from personnel detail → deep link into All Tasks — one screen, two entries.
- Admin CRUD on mobile: build read-only first (it's 90% of usage); add create/update only if someone actually complains from the field.
