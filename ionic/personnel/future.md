# Personnel — Future (deferred)

- **Shift calendar per person** — personnel detail shows upcoming shifts; needs `GET /shifts?user_id=` (backend add already planned) — cheap add after attendance backend lands.
- **Personnel import** — web-only feature; not in app scope.
- **Direct messaging** — chat between manager/cleaner; no backend concept.
