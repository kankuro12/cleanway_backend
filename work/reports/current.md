# Reports & Hardening — Current

## Done

- Nothing (module not started; depends on modules 1–5).

## In Progress

- Nothing.

## Next

1. Dashboard widgets service (per-role queries, minimal columns).
2. Report service + filters + queued export (`export_jobs` table).
3. Audit viewer UI.
4. Settings tables + cached config service + settings admin UI.
5. API: Resources, envelopes, pagination, rate limits, token revocation, OpenAPI.
6. Hardening pass: query/index audit, CORS, upload validation, security review.
7. E2E test suite for spec §24.4.