# Properties — Future

- Map view + nearby-property search (needs spatial index — MySQL spatial columns when DB permits).
- Saved filters for property search.
- Bulk selection + bulk export.
- Property documents/images modules (attachments storage).
- Auto-convert one-time task locations into saved properties (task phase).
- Google re-geocoding on address change detection (when address edited materially).
- Service frequency scheduling integration (recurring task templates reference properties).
- Import of properties from CSV with queued geocoding.