# Foundation — Future

Deferred, will build later:

- Password reset / "forgot password" flow.
- Multi-factor authentication.
- Session management UI (list/revoke web sessions).
- Device/session management admin screen (spec §5.1 "manage mobile devices and sessions") — needed when API + mobile land.
- API version documentation + OpenAPI spec (with reports phase).
- Redis as default queue (dev uses `database` for zero-infra).
- Backup/restore verification runbook.
- Email/notification transport config once notifications module exists.