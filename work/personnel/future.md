# Personnel & Teams — Future

- Skills/certifications matching for task assignment (spec §9.3 "required skills").
- Service areas model (currently suggested as JSON field; promote to table when scheduling needs it).
- Employment types table (if business needs custom types beyond enum).
- Invite email with activation link + password set page.
- Bulk import/export of personnel.
- Onboarding checklist per employee.
- Leave tracking module (needed by task conflict validation — build before tasks phase if leave conflicts are required there).